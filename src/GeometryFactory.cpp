#include "GeometryFactory.h"

GeometryFactory::MeshData GeometryFactory::CreateBox(float width, float height, float depth)
{
    MeshData meshData;

    return meshData;
}

GeometryFactory::MeshData GeometryFactory::CreateSphere(float radius, int levels, int slices)
{
    MeshData meshData;

    return meshData;
}

GeometryFactory::MeshData GeometryFactory::CreateCylinder(float radius, float height, float slices)
{
    MeshData meshData;

    return meshData;
}

GeometryFactory::MeshData GeometryFactory::CreateCylinderNoCap(float radius, float height, float slices)
{
    MeshData meshData;

    return meshData;
}